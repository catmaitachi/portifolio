/**
 * Nomes sorteáveis para quem não quiser dar o próprio.
 *
 * É a lista padrão — sobrescreva sem tocar no código pela variável
 * `VITE_RANDOM_NICKNAMES` (separada por vírgulas).
 *
 * Critério dos nomes: minúsculos e sem espaço, porque viram o prompt do
 * terminal (`sombra:$`).
 */
export const randomNicknamesSeed: string[] = [
  'blinky',
  'pinky',
  'inky',
  'clyde'
];
